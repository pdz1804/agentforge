# AgentForge API — Phase 0 slice.
# agent-core is installed separately as an editable local package:
#   pip install -e ../../packages/agent-core
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pytest>=8.0
PyJWT==2.13.0
# Optional agent-core extra: MCP server discovery/adaptation (StdioMCPConnector).
# Only imported lazily inside discover(); listed here so the API's env has it
# available for the "everything" server registered by build_default_registries.
mcp>=1.0
